# 구현 계획 — 월드맵 토지정보 추출기 v1 (MODE-A2)

- 작업 ID: 20260801-worldmap-land-scan
- 기준선: **요구사항 v3 / 설계 v4 (2026-08-02 승인, DCR-004 — 좌표 무결성: 주기 재앵커 + 지연 기록)**
- 최종 갱신: 2026-08-02
- 이 문서는 wf-implement의 작업 기록이며 진행 상태를 여기서 갱신한다. **세션을 새로 시작하면 이 문서를 가장 먼저 읽는다.**

## 현재 상태 요약 (재개 지점)

| 구분 | 상태 |
|---|---|
| 요구사항·설계 | 요구사항 **v3** / 설계 **v4 승인 (2026-08-02, DCR-004)** — 좌표 무결성(K=3팬 클릭 재앵커 + 지연 기록·드리프트 보간). NFR-02 = 48시간(재앵커 포함 기준선 39~43시간). DCR-002·003 반영 완료 |
| 스파이크 | S-1·S-2 완료. **S-3 완료**(입력란 조작·클램프 감지), **S-4 완료**(팝업은 닫을 수 없음 → 겹침 커버 유지), **S-5 완료·통과**(드래그 팬 실현 가능 — 아래 확정 사실 15~19, `spikes/s5_drag_pan/findings.md`), **S-6 재측정 완료**(방문 99회 — 광폭 팬 2.95s/68.2타일, 총 33~36시간, `spikes/s6_remeasure/findings.md`) |
| 구현 완료 | T8 골격, T10 store, T9 win, T11 vision, T12 nav, **T13 controller/cli**, 부가 도구. T14: 이중 앵커 기각·팬 텔레메트리·점프 GO 재클릭·수면 안정화 폴백 + **DCR-004 재앵커·지연 기록(controller v4) 구현·실기 검증 완료** |
| **다음 작업** | **전체 스캔(착수 전 사용자 확인 필수)** → NFR-02 판정 → AC-02 재대조(중립형 철광·석재 템플릿 확충 후) → T15. **NFR-02 주의신호**: 지정 영역 실측 7.9~9.7타일/s(S-6 기준선 23.1의 ~40%) — 재앵커 오버헤드 + 서부 행 짧음 + 조기 종료 낭비. 이 속도면 전체 ~75h > 48h. 착수 전 개선 검토: 재앵커 주기 K 3→4·5 튜닝(DCR-004 파라미터), 판독 실패 잔존 보강, 중부 행 처리율 재측정. AC 수행 결과는 검증 표(AC-01~07) 참조 |
| 테스트 | `python -m unittest discover -s tests` — **99건** 전부 통과 (환경 주의: `.venv`에 `pip install -e . --no-deps` 필요) |
| 실기 확정값 | **맵 크기 1619×1619**(자동 감지 성공), 스캔 창 클라이언트 2544×657 |
| 테스트 클라이언트 | **계정 羊커리** (2026-08-02 기준 hwnd `0x60042`). HWND는 재시작 시 바뀌므로 `mapscan windows --crops <dir>`로 계정명 크롭을 저장해 확인한다 |
| 커밋 | main 브랜치, origin 푸시 완료 |

실행 전제: 게임 클라이언트가 관리자 권한으로 실행 중이므로 **도구도 관리자 권한 필요**. 스캔 창은 종횡비 3 이상(사분면)으로 맞춘다.

## 기준선

- 관련 요구사항: [requirements.md](requirements.md) v3 — FR-01, FR-02, FR-03b, FR-05, FR-07~12, NFR-01~06
- 관련 설계: [design.md](design.md) v3 (2026-08-02 팬 이동 전략·S-5/S-6 실측 반영)
- 관련 ADR: ADR-001(스택), ADR-002(캡처·입력 + T12 실측 보완), ADR-003(저장), ADR-004(모드 구조), ADR-005(창 종횡비 + 실측 보완), **[ADR-006](decisions/ADR-006-map-size-detection.md)(맵 크기 감지 — 신규)**
- 관련 DCR: [DCR-001](changes/DCR-001-mode-a-zoom-limits.md) 승인 — v1 구현 범위는 **MODE-A2만** / [DCR-002](changes/DCR-002-scan-throughput.md) 반영 완료(안 d) / **[DCR-003](changes/DCR-003-nfr02-final-pan-design.md) 승인(2026-08-02) — NFR-02 48시간 확정 + 팬 이동 설계 v3** / **[DCR-004](changes/DCR-004-reanchor-coordinate-integrity.md) 초안(2026-08-02) — 좌표 무결성(주기 재앵커 + 지연 기록), 승인 대기**
- 구현 중 수단이 바뀐 항목(경미한 변경)은 [design-change-log.md](design-change-log.md)에 이유와 함께 정리했다.

## 스파이크 확정 사실 (구현 제약)

| # | 사실 | 근거 |
|---|---|---|
| 1 | WGC로 가림 상태 캡처 가능. PrintWindow는 검은 프레임(사용 불가) | S-1a |
| 2 | 게임이 elevated면 도구도 관리자 권한 필요(PostMessage/SetWindowPos) | S-1b |
| 3 | 캡처 세션은 **HWND 바인딩** 필수(동일 제목 다중 창). `window_hwnd` 인자를 쓴다 — 제목 매칭 + z-raise 우회는 같은 크기 창을 구분 못 해 실패했다 | S-1a, T12 |
| 4 | 전략 뷰는 중립 타일 미렌더링 → 자원 종류는 디테일 뷰에서만 | S-2 |
| 5 | 가시 타일 수 ∝ 창 종횡비. 사분면(2544x657)에서 격자상 246타일, 프레임에 온전히 들어오는 셀은 **199개** | S-2c, T11 |
| 6 | 스캔 창에서 디테일 줌 최종 단계 = 최대 축소(한 노치 더 = 전략 뷰) | S-2c |
| 7 | 클릭↔좌표 대응 데이터: (700,350)→(1012,629), (2100,350)→(1020,620) @2544x657 | S-2c |
| 8 | 타일 격자는 **일반 평행사변형**. `E_MX=(99.5,49.0)`, `E_MY=(-55.5,45.0)` @2544x657 | T11 |
| 9 | 점프 후 앵커는 **고정 화면 위치**(클라이언트 1225,337). 선택 하이라이트는 점프 시 신뢰 불가 | T12 |
| 10 | 좌표 입력란은 **블러 시점에 클램프**. 백스페이스는 `VK_BACK`(+`VK_END` 선행) 필요, `WM_CHAR('\b')`는 무시됨 | S-3 |
| 11 | **ESC 금지** — 일반 뷰에서 게임 종료 확인 창이 뜬다. 선택 팝업은 닫을 수 없다 | S-4 |
| 12 | 지도 UI 클릭 전 **화면 상태 검증 필수** — 미검증 클릭으로 행군 화면이 열린 사고 발생 | T12 |
| 13 | 맵 최대 좌표 **1619×1619** (자동 감지, 계정 2개 재현) | S-3 |
| 14 | UI 좌표는 계정과 무관, 창 크기에만 의존 | T12 |
| 15 | **점프 완료 후 게임은 지도 UI를 닫고 디테일 뷰로 복귀** — 팬·분류·클릭은 디테일 뷰에서. 조작 전 검증은 계정 HUD 시그니처 + 지도 모드 아님 | S-5 |
| 16 | **PostMessage 드래그로 디테일 뷰 팬 동작.** 드래그는 타일을 선택하지 않고, 점프 팝업은 첫 팬에서 사라진다. 감속·관성 없음(릴리스 +0.5s 잔여 0px), 좌우 대칭, 같은 y대역 재현성 σ≤5px | S-5 |
| 17 | **팬 이동장은 화면 y에 선형 의존**(원근 틸트 지면: 명령 520px에 y127→444 / y311→482 / y466→517px). 그리드 기저 E_MX/E_MY는 **국소값** — 이동 추적·분류는 관심 y대역으로 한정해야 한다 | S-5 |
| 18 | 선택 팝업은 타일 **우측 +355px(점령·행군 버튼)까지 확장** — `ui.POPUP_RECT_REL`(+160)은 과소. 드래그 시작·종점은 팝업 영역 밖 필수, 커버 오프셋 재검토 필요 | S-5 |
| 19 | 겨울(눈) 테마 팝업 좌표 폰트는 기존 글리프 템플릿과 매칭 약함 — 소스별 변형 템플릿 보강 필요(T14) | S-5 |

## 계획

### 1부 — 스파이크 (완료)

- [x] T1. 개발 환경 준비 (.venv, pillow/numpy/opencv/windows-capture)
- [x] T2. S-1a 백그라운드 캡처 — 성공(WGC)
- [x] T3. S-1b 백그라운드 입력 — 성공(관리자 권한 필요)
- [x] T4. S-1 판정 기록 + ADR-002 확정
- [x] T5. S-2 줌·자원 식별·그리드 → P-01 실패, DCR-001 발행·승인
- [x] T5b. S-2b/c 창 크기·종횡비 → ADR-005
- [x] T6. S-3 좌표 입력란 숫자 판독 — **P-03 실패**, 대신 클램프 이미지 비교로 FR-02 달성 → ADR-006
- [x] T7. S-4 팝업 오클루전 — **팝업 닫기 불가**(ESC는 종료 확인 창), 겹침 커버 유지 → DCR-002의 근거
- [x] T7b. S-5 드래그 팬 실현 가능성 — **통과** (2026-08-02). 3게이트 충족: 팬 동작·선택 미유발, 이동량 재현 σ≤5px·관성 없음, 드리프트는 대역별 이동량으로 모델링 가능(무작위 누적 아님)·재점프 재앵커 정상. 발견·제약은 `spikes/s5_drag_pan/findings.md`
- [x] T7c. S-6 재측정 — DCR-002 안 (d) 최적화 3종(팬 전환·대역 한정 분류·HUD 축소) 적용, 방문 99회 연속 실측 (2026-08-02). **광폭 팬 2.95s/68.2타일, 행 시작 점프 6.7s/51타일 → 총 33~36시간** (좌표 점프 단독 150시간 대비 4.2~4.6배). [DCR-003](changes/DCR-003-nfr02-final-pan-design.md) 발행. 하네스 `spikes/s6_remeasure/measure100.py`는 T13 참조 구현

### 2부 — 제품 구현 (MODE-A2)

- [x] T8. 프로젝트 골격 (src/mapscan, pyproject, tests)
- [x] T10. store 계층 — DataStore(WAL, upsert, 체크포인트), export_csv. 테스트 6건 통과
- [x] T9. win 계층 — **완료.** WindowSession(HWND 확정·권한 점검·스캔 창 설정/복원), WgcCapture(z-raise 바인딩 + 첫 프레임 크기 검증), PostMessageInput(클릭·휠·문자). CLI `windows`/`probe` 점검 명령과 `run_elevated.ps1` 러너 포함. 단위 테스트 14건 + 실기 probe 통과
- [x] T9b. 부가 도구 — `tools/set-client-quadrant.ps1` (독립 실행형 창 크기 조절: 사분면/지정 크기/전체 일괄/복원). 요구사항 산출물은 아니며 수동 운용 편의용
- [x] T11. vision 계층 — **완료.** GridMapper(평행사변형 격자·하이라이트 앵커·가시 셀 열거), TileClassifier(점령색 판별 + 템플릿 매칭 + 지형 휴리스틱 + 미상 보수 처리), DigitReader(글리프 판독), 템플릿 자산(`assets/templates/`), CLI `classify` 점검 명령. 단위·회귀 테스트 20건 추가(총 40건). 상세는 아래 "작업 결과 — T11"
- [x] T12. nav 계층 — **완료.** Navigator(지도 모드 검증·좌표 점프·안정화·맵 크기 감지), ScanPlanner(잉여류 타일링 방문 계획·보충·재개), UI 캘리브레이션 상수, S-3/S-4 스파이크. 상세는 "작업 결과 — T12"
- [x] T13. controller/cli — **완료** (2026-08-02). 행 기반(설계 v3 §4.3) ScanController + DetailScan(A2, A1/B/C는 NotImplemented), Watchdog(빈·검은 프레임·크기 변경), 진행률(행·커버·ETA)·체크포인트 재개, CLI `scan`(--max-rows/--start-row/--max-pans 부분 실행). 신규: `nav/pan.py` PanTracker(전단 적합 + 점수 하한 0.08·기대 창 ±150px·leave-one-out·기울기 물리 범위 4중 기각), `Navigator.pan`+디테일 뷰 시그니처 검증, `watchdog.py`, ui.py v3 상수. 상세는 "작업 결과 — T13"
- [ ] T14. 검증 — **진행 중** (상세: "작업 결과 — T14"). 완료: 기각 튜닝·경계 앵커(서변)·기저 실측·DCR-004 구현·글리프 보강·표본 라벨링·점령형 템플릿·파랑/초록 오탐 교정·FR-07 병합·**지정 영역 스캔(행 148~165, 14,972타일)·AC-01/03/06 통과·AC-05/07 부분·AC-02 부분(오분류 1 잔존)**·사막 판독 복구·강제 종료 창 복원 수정. 잔여: 전체 스캔(사용자 확인) → NFR-02 판정(주의신호 — 처리율 40%), AC-02 재대조(중립형 템플릿 확충 후) |
- [ ] T15. 통합·자체 리뷰·최종 보고, README 갱신
- [x] T15a. 문서 일괄 갱신 (2026-08-02) — requirements/design/ADR-002·005/README/스파이크 findings에 T11·T12 실측 반영, ADR-006·DCR-002 신규, 변경 이력에 경미 변경 표 추가

### 작업 결과 — T14 (진행 중, 2026-08-02)

T14.1(기각 튜닝) 원인 규명 중 **추측항법 좌표 드리프트**를 발견 —
[DCR-004](changes/DCR-004-reanchor-coordinate-integrity.md) **승인(2026-08-02)** →
설계 v4 갱신, controller에 **주기 재앵커 + 지연 기록(드리프트 팬별 보간 보정)** 구현.
단위 6건 추가(총 84건), 행 150 실기 검증 통과: 재앵커 3회 적용(드리프트 −1..−2,+4..+5),
오판독 2회를 새니티 창이 차단(좌표 오염 0, 꼬리 216타일 보수 폐기), 620타일 보정 기록, 47s.
증거·도구는 `spikes/t14_pan_tuning/` (capture_row·analyze·survey·pilot_reanchor + findings.md).

- 반영(경미, 기준선 내): `nav/pan.py` **이중 앵커 기각**(게인 ±250 또는 직전 실측 ±150 —
  직전 단일 앵커 ±150이 정상 측정을 버려 행 150 조기 종료를 유발했음, `ui.PAN_GAIN_TOLERANCE_PX` 신설),
  `controller.py` 팬별 raw 텔레메트리 INFO 로그, `nav/navigator.py` **점프 GO 재클릭**(팝업 열림 시
  GO 무시 — 보충 방문 연속 점프 경로 결함) + **수면 애니메이션 고정 대기 폴백**(강 지역 StabilizeTimeout).
  테스트 4건 추가(총 78건)
- T14.2(경계 앵커): **서쪽 변 (0,922) 클램프 없음 확인**(판독 5/6 정합 ≤1타일). 모서리·타 변은 미확인
- T14.4(기저 실측): 기저 y-성분의 **지역 의존 확인** — 아래 확정 사실 26. BAND_Y 확대 판단은 DCR-004(재앵커) 반영 후 재평가
- T14.3(템플릿): 테마가 겨울→봄으로 변경됨. 봄 팝업 좌표는 기존 글리프로 ~85~90% 판독
  → 변형 글리프 9종 보강 + 최빈값 투표로 해소(사실 32). **점령형 표본 10건 수집·라벨링 완료**
  (`sample_tiles.py`) → 템플릿 iron_occ·food_occ 등록 + 파랑 오탐 교정(사실 37~39).
  내땅(초록) 테두리 표본은 미확보 — 분류 임계 잠정 유지(사실 13)

**T14 실기로 확정된 사실:**

| # | 사실 | 영향 |
|---|---|---|
| 26 | **기저 E_MX/E_MY의 y-성분은 맵 지역 의존** — NW에서 u-스텝당 c-방향 ~2.5px 오차. 한 화면 안은 ±1타일이나 행 이동 누적 시 팬당 my +1.3~1.7타일. 행 150에서 10팬 후 실측 (mx +2..+11, my −11..−21)타일 | 추측항법만으로는 좌표 기록 불성립 → DCR-004(주기 재앵커 + 지연 기록) |
| 27 | **dx(y) 이동장은 지형 고도(시차 레이어)로 ~±100px 계단 불연속** — 3밴드 선형 적합 기울기 진동(-0.09~-1.15), LOO가 정직한 밴드를 기각하기도 함 | 재앵커 도입 후 추적 밴드 재설계 필요성 재평가 |
| 28 | **강(수면 애니메이션) 지역**: 추적 NCC 0.03~0.3, `wait_stable` 상시 시간초과 | 점프 폴백 구현 완료. TrackLost 잔존은 재앵커로 복구 |
| 29 | **선택 팝업이 열린 채 GO 클릭은 무시된다**(지도 모드 잔류) | `Navigator.jump` GO 재클릭 구현 완료 |
| 30 | 봄 테마 팝업 좌표 판독 성공률 ~85~90%, **"3"↔"8" 오판독 실례 1건** | 재앵커 판독에 새니티 창 필수(파일럿 구현). T14.3에서 글리프 보강 |
| 31 | **주기(3팬) 클릭 재앵커 파일럿 성공** — 드리프트 (−1..−2,+4..+5)/3팬 흡수, TrackLost 변위 유실(+11)도 다음 사이클 복구, 재앵커 적용 시 잔여 ≤1타일 | DCR-004 권고안의 실증 근거 → **승인·구현 완료** |
| 32 | 팝업 좌표 오판독은 **타일별로 결정적**이다("3"→"8"이 같은 타일에서 반복). 원인은 글리프 절단면 + 첫 파스 채택 | **해소(T14.3)** — 봄 변형 글리프 6종 추출(`extract_glyphs.py`) + 판독을 슬라이드 **최빈값 투표**로 변경. 행 150 재실행에서 같은 지점 정상 판독 |
| 33 | **절벽 클릭 피킹은 수 타일 어긋난다** — 절벽 팝업 실측 (1018,622) vs 격자 예측 (1020,617), Δ(−2,+5). T11 사실 #12(1타일)의 확장 | 재앵커 대상은 이번 사이클 분류된 **평지(resource) 셀 우선**으로 선택(controller). 새니티 창은 팬당 3타일로 완화(전단 국소차 실측 9타일 커버) |
| 34 | **일시적 게임 활동(부대 행군·모집 알림)이 추적을 전면 붕괴시킬 수 있다** — 같은 명령·같은 행이 한 실행에서만 전 팬 TrackLost(점수 0.03~0.28) | 안전 동작 확인: 행 조기 종료·오염 기록 0(보수 폐기). 행 실패는 재시도·보충 몫이므로 전체 스캔에 치명적이지 않음 |
| 35 | 점령형 팝업 폰트의 **'0'은 좌우 획 2성분으로 쪼개지고**(내부 간격 1px = 글리프 간 간격과 동일) 끝의 **"0)"는 맞닿아 한 클러스터로 병합**된다 — (187,793)류 오판독·판독 실패(None)의 실체. 간격 규칙만으로는 분리·병합 판정 불가 | 오판독 자체는 새니티 창이 차단(무해). 판독률 개선은 **세그먼테이션 개선(높이 인지 1px-간격 병합 + 과폭 클러스터 분할)** 후속 과제. 재앵커 실패 스트립은 `output/evidence/`에 자동 저장되어 표본이 축적된다(`controller._save_evidence`) |
| 36 | **다중 타일 도시는 어느 셀을 클릭해도 기준 좌표 하나로 보고된다** — 셀 (1013,625) 클릭 → 팝업 (1016,626), HP·주군·동맹 표시 | FR-07 병합의 실기 근거. 재앵커가 도시 위를 클릭하면 기준좌표-예측 차가 새니티 창을 위반할 수 있으나 평지(resource) 우선 선택이 이미 회피 |
| 37 | 비점령 공터가 **ally(파랑)로 오탐**되는 표본 확보 — (345,336) 인접 파랑 번짐 추정 | **해소** — 사실 38의 마주보는 변 쌍 규칙으로 교정(회귀 `tiles_mine_00_cell.png`). 절벽·강 인접 오탐(occ#4, 팝업 진실 "절벽 — 점령 불가")도 동일 규칙으로 교정 |
| 38 | **파랑 테두리는 분율만으로 오탐과 분리 불가**(정탐 6~12% vs 오탐 8.6/7.3% — 겹침). 변별력은 측면 분포: 정탐은 마주보는 변 쌍 양쪽, 오탐(강·인접 번짐)은 한쪽 인접 변에만(반대 변 정확히 0.0000). 정탐 최저 변 0.0199 | `classifier._opposite_sides` 마주보는 변 쌍 규칙(변당 0.015) 도입. 표본 10건 + 기존 픽스처 4셀로 확정(`analyze_tiles.py`). 빨강(적)은 오탐 없음 — 임계 유지. 잔존 한계: 소유 클러스터 내부 타일(테두리 없음)은 중립 판정될 수 있음(기존 한계 유지, 사실 11) |
| 39 | 점령형 스프라이트 템플릿 2종 실기 등록 — **철광**(iron_occ, "Lv.5 철광" 팝업 진실)·**식량 밭**(food_occ, "Lv.5 식량"). 매칭 시 점령 타일도 종류 판정(이전 일괄 미상) | `TEMPLATE_KINDS` 갱신, 회귀 5건(`FieldSampleRegressionTest`). Lv.8 철광 표본은 월드 라벨 텍스트 겹침으로 등록 보류. 내땅(초록) 표본은 여전히 미확보 — (350,330) 일대는 자기 도시가 아니었음 |
| 40 | **주성 성채(키프) 템플릿 검출은 NCC 분리가 확실하다** — 자기 매칭 1.0 vs 무관 지형 차순위 ≤0.25(occ·mine 두 프레임). 성벽 지면 다이아몬드 중심은 키프 원점 +(60,38). 단 다른 레벨·스킨 도시(같은 프레임 내 소형 도시들)는 이 템플릿에 반응하지 않음 | FR-07 구현: `detect_structures`(임계 0.6) + 반경 0.75타일 멤버 병합 + center_x/y 추정 표기. 미검출 도시는 미상 유지(NFR-04 보수 원칙). 도시 스킨별 템플릿 보강은 후속 |
| 41 | **서량(사막) 지역에서 팝업 좌표 판독이 붕괴한다** — 재앵커 실패 코퍼스 27장 중 성공 2. 원인: 이 렌더링의 '('가 '1'로 일관 오매칭 + '0' 분리·"(1" 병합(사실 35 유형). 행들이 재앵커 연속 실패로 팬 ~6에서 조기 종료 | **복구(e06f829)** — 정렬 신뢰 소스에서 paren_l 변형 3종 수확 + 세그먼테이션 복구 패스(`digits.read(repair=)` — 병합·분할 가설을 매칭 점수로 채택). 코퍼스 2/27→15/27, 행 158이 가림 상태로 50+팬 완주. 잔여 실패는 남아 행 161·165 조기 종료 — 증거 자동 축적으로 점진 보강. 일괄 수확은 정렬 밀림 시 오라벨 템플릿을 만들므로 금지(재검증 롤백 실증) |
| 42 | **클릭 피킹은 평지에서도 my±1이 빈발한다**(AC-01 대조 26클릭 중 ±1 어긋남 9) — 절벽(사실 33)만의 문제가 아님. 기록 좌표 자체는 이웃 셀 조회로 정합 확인 | AC-01 대조는 팝업 진실 좌표의 DB 기록으로 판정. 재앵커 새니티 창은 이미 이를 흡수(±2 기본) |
| 43 | **중립형 자원 실기 확인 3종** — ① Lv.1 식량: food_occ 템플릿이 정당 매칭(중립형·점령형 밭 스프라이트 유사) → 식량/neutral 30건 정탐 ② Lv.1 철광 (185,794)를 **공터로 오분류**(작은 광석 스프라이트가 공터 휴리스틱 통과 — 유일한 확정 오분류) ③ Lv.3·Lv.5 석재 육안 확인(미상 보수 처리 — 정상) | 철광 오분류는 중립형 철광 템플릿 확보로 해소 예정(AC-02 재대조 필요). 석재도 템플릿 후보 스프라이트 위치 확보(`ac01_a_02`·`ac01_c_07` 팝업) |
| 44 | **무주 공터가 mine(초록)으로 오탐 2건** — 팝업 진실 "공터 (181,801)"·"공터 (115,825)". 재채집 프레임에서는 초록 0%(중립 판정) → 스캔 중 팬 진행 밴드 위치 오차로 **인접 밭의 밝은 초록이 샘플링**된 것 | green에도 마주보는 변 쌍 규칙 적용(blue와 동일 — 인접 번짐은 한쪽 변). 내땅 정탐 표본이 없어 보수 적용에 손실 없음. 픽스처 `minefp_*_cell.png` + 합성 회귀 |

### 작업 결과 — T13 (완료, 2026-08-02)

- 산출물: `src/mapscan/controller.py`(plan_rows·DetailScan·ScanController), `src/mapscan/watchdog.py`, `src/mapscan/nav/pan.py`(PanTracker), `nav/navigator.py`(pan·디테일 뷰 시그니처), `nav/ui.py`(v3 상수), CLI `scan`. 테스트 2파일 10건 추가(총 74건)
- 실기 완료 조건 실증 (羊커리 0x60042, 관리자 권한): `mapscan scan --start-row 150 --max-rows 1 --max-pans 12` → 점프 재앵커 → 광폭 팬 10회 → **750타일 분류·기록 38초**, 창 배치 자동 복원. 행 268(경계 인접)은 전단 기울기 안전망이 오추적을 감지해 조기 종료(확보 99타일 유지)

**T13 실기로 확정된 사실:**

| # | 사실 | 영향 |
|---|---|---|
| 22 | **경계에서 시작하는 행은 팬 추적이 실패한다** — 화면 절반이 맵 밖(정적 무지물)이라 추적 템플릿이 오염된다 | `plan_rows`가 시작점을 스트라이프 방향으로 ~15타일 인셋. 빠진 경계 구간은 보충 방문 몫 |
| 23 | 광폭 팬(1900px)의 팬별 이동 변동은 ±100px대 — 기각 창 ±80은 정상 측정을 버린다 | 허용 창 150px·점수 하한 0.08로 완화 + 전단 기울기 물리 범위(−1.5~0.3) 검사 추가 |
| 24 | 행 중간 TrackLost는 결정적(같은 내용 → 같은 실패)이라 재시도가 무익 | 행 조기 종료로 전환(확보 타일 유지, 잔여는 겹침·보충 커버). 행 실패 재시도는 점프·뷰 검증 실패에만 적용 |
| 25 | 중앙부에서도 간헐 TrackLost 잔존(행 150 팬 #11, 밴드 점수 0.71도 포함된 채 기각) — 원인 미규명 | **T14 튜닝 항목**: 스캔 로그의 raw 텔레메트리로 기각 규칙 정밀화. 조기 종료 빈도가 보충 부담(총 소요)을 좌우 |

- 남은 한계: ~~FR-07 2칸 건물 병합(`center_x/y`) 미구현~~ → **T14에서 구현 완료**(사실 40 — 주성 검출 + 멤버 병합). 경계 좌표 점프의 앵커 신뢰성은 서쪽 변만 확인(모서리·타 변 미검증)

### 작업 결과 — T11 (완료, 2026-08-02)

- 산출물: `src/mapscan/vision/` (grid.py, classifier.py, digits.py), `assets/templates/` (tiles/wood.png, digits/ 글리프 + README에 출처 기록), CLI `classify`, 테스트 3파일 20건
- 완료 조건 실증: `mapscan classify --image snap_tr_base.png --anchor 1020 620 --anchor-image snap_click_R.png` → 가시 타일 200개 분류·CSV 산출(1.1초). 지상 진실 5개 좌표 스팟 체크 전부 일치

**구현 중 실측으로 확정된 사실 (T12 이후 반영 필수):**

| # | 사실 | 영향 |
|---|---|---|
| 8 | 타일 격자는 2:1 등각 마름모가 아니라 **일반 평행사변형 격자**. 스캔 창 실측 기저 `E_MX=(99.5,49.0)`(+mx=우하), `E_MY=(−55.5,45.0)`(+my=좌하). 계획서의 `a≈82.4, b≈a/2` 공식은 폐기 | GridMapper는 일반 기저로 구현(설계 §2 책임은 불변 — 경미 변경). 기저는 창·줌별로 달라지므로 T12에서 2회 점프 자기 캘리브레이션 권장 |
| 9 | **선택 하이라이트(크림색 링, HSV S<100·V>195) = 타일 경계와 정확히 일치**(인셋 없음). 앵커에서 1900px 거리의 도시 타일 테두리와도 수 px 정합 | 앵커 보정 신뢰 가능. `find_selection_highlight` 구현 완료 |
| 10 | **점령된 자원 타일은 스프라이트가 생산 시설(밭·목장·채석 시설 등)로 바뀐다** — 중립형(나무 군락 등)과 완전히 다름 | 템플릿을 중립형·점령형 2벌 수집해야 함(T14). 확보 전에는 점령 타일 종류=미상 |
| 11 | 점령 테두리 색 실측(edge-band 분율): 빨강(적군) 2~3% vs 비점령 0%, 파랑 계열 7~11% vs 0%. 단 **테두리 인셋이 스프라이트마다 달라(0.36~0.46)** 내측 밴드(0.30~0.42)로는 일부 점령 타일(빈 밭 등)을 놓침 | 알려진 한계로 기록. T14 표본으로 밴드·임계 재조정 |
| 12 | 절벽(특수 지형) 클릭 좌표는 지형 고도 시차로 화면상 격자보다 1타일 어긋날 수 있음(클릭L 실측). 팝업 좌표 폰트는 창 크기와 무관하게 동일하나 렌더링별 AA 차이로 글리프 교차 매칭 약함 → 소스별 변형 템플릿 | DigitReader는 변형 최대 점수 방식. 좌표 입력란 폰트는 S-3(T12)에서 수집 |
| 13 | 내땅(초록) 테두리 표본 미확보 — 잔디(H35~85, S중앙값~100)와 혼동 위험이 있어 고채도·고명도 잠정 임계. 동맹/우호 파랑 분리(V 170)도 잠정 | T14 점령상태 5종 표본 대조(AC-02)에서 확정 |
| 14 | 산(절벽)은 상부가 잔디라 색 구성만으로 판별 불가, 모래 평지는 지상 진실 미확인 → 모두 **미상으로 보수 처리** | 오분류보다 미상이 안전(NFR-04). T14에서 실기 클릭 표본으로 휴리스틱 확정 |

- v1 분류 능력: 목재(중립형)·공터·강(잠정)은 종류 판별, 점령상태는 빨강/파랑 계열 검출. 그 외 미상 + 신뢰도. 템플릿 잔여 목록은 `assets/templates/README.md`
- 성능: 프레임당(가시 200타일) 분류 약 1.1초 — 방문당 예산(2.5~4초) 내

### 작업 결과 — T12 (완료, 2026-08-02)

- 산출물: `src/mapscan/nav/` (navigator.py, planner.py, ui.py), `assets/templates/ui/map_mode.png`, 스파이크 `spikes/s3_nav_ui/` (상주 에이전트·실기 검증·S-4 실험), 테스트 2파일 24건
- 실기 완료 조건 검증 (관리자 권한, hwnd 0x40148): 지도 모드 자동 진입 → **맵 크기 (1619,1619) 자동 감지 39.7초** → 좌표 (1020,620) 점프 4.3초 → 앵커 (1226,368) → 가시 199타일 분류 3.4초 → 방문 계획 생성

**T12 실측으로 확정된 사실:**

| # | 사실 | 영향 |
|---|---|---|
| 15 | **WGC 제목 매칭은 같은 크기·같은 제목 창을 구분하지 못한다.** z-raise 후 제목 매칭 방식이 조용히 다른 창을 잡아, 입력은 A창에 가고 화면은 B창을 보는 상태로 30분간 오진단했다. `windows-capture` 2.x의 `window_hwnd`로 **HWND 직접 바인딩**하도록 수정 | `WgcCapture` 수정 완료. 크기 검증(`BIND_TOLERANCE`)은 보조 수단일 뿐임을 명시 |
| 16 | **점프 후 선택 하이라이트 링은 신뢰할 수 없다**(약하거나 지연되어 나타남). 대신 게임이 대상 타일을 **고정 화면 위치**에 놓는다 — 클라이언트 (1225,337) | 앵커는 `ui.JUMP_ANCHOR` 고정값 사용. 템플릿 매칭으로 교차 검증(y 오차 0.6px) |
| 17 | 좌표 입력란은 **블러(포커스 이탈) 시점에 클램프**된다. 타이핑 중에는 5자리까지 그대로 표시 | `_render_of`가 반대 필드를 클릭해 블러시킨 뒤 읽는다 |
| 18 | 백스페이스(WM_CHAR `\b`)는 무시된다. **WM_KEYDOWN/UP VK_BACK**은 동작하되 커서 왼쪽만 지운다 → **VK_END 선행 필수**. 생략하면 잔여 문자가 남아 엉뚱한 좌표로 이동한다(실제 발생) | `PostMessageInput.key()` 추가, `_type_into`가 END+백스페이스 |
| 19 | 입력란 폰트는 숫자 글리프가 **서로 붙어** 있어 연결 성분 분리가 실패한다(팝업 폰트와 다름) | OCR 대신 **클램프 렌더링 이미지 비교 이진 탐색**으로 맵 크기 감지 — 폰트 비의존이라 게임 업데이트에 강함 |
| 20 | **ESC는 위험하다.** 일반 뷰에서 누르면 "게임을 종료하시겠습니까?" 확인 창이 뜬다(실측, 취소로 복구). 선택 팝업은 닫히지 않는다 | S-4 판정: 팝업 닫기 불가 → **설계대로 인접 방문 겹침으로 커버**(P-06 유지). 제품 코드에서 ESC 미사용 |
| 21 | **오클릭 사고 실증:** 지도 모드가 아닌 상태에서 지도 모드 좌표를 클릭해 **행군(부대 이동) 선택 화면**이 열렸다(ESC로 취소, 행군 미실행) | `Navigator._click_verified`가 **매 클릭 전 지도 모드 마커를 확인**한다. 마커 점수 지도 모드 1.00 vs 일반 뷰 0.10~0.25로 분리 |

**R-09 (신규 위험) — NFR-02 스캔 소요 목표 초과**

실측 기반 재산출: 방문당 커버 가능 타일이 예상보다 훨씬 적다.

| 항목 | NFR-02 가정 | T12 실측 |
|---|---|---|
| 화면당 가시 타일 | 246 (HUD 제외 유효 185) | 199 |
| HUD·팝업 제외 후 커버 오프셋 | — | **56** |
| 방문당 신규 커버(잉여류 보폭) | 185 | **36** (보폭 6×6) |
| 맵 크기 | 1600² 가정 | **1619² 실측** |
| 방문 횟수 | 13,900 | **72,900** |
| 방문당 소요 | 2.5~4초 | 점프 4.3초 + 분류 3.4초 = **7.7초** |
| **총 소요** | **10~15시간** | **약 150시간** |

원인: ① 오클루전(특히 선택 팝업 760×440px, 우측 패널, 하단 밴드)이 커버 영역을 크게 깎는다 ② 커버 집합에 구멍이 있어 잉여류 타일링 보폭이 작아진다 ③ 방문당 소요가 가정의 2배.

개선 여지(미착수, T13/T14 후보): 분류를 신규 커버 36타일로 한정(3.4초 → ~0.7초), 조작 지연 축소, HUD 사각형을 실제 불투명 영역으로 축소, 좌표 점프 대신 드래그 팬으로 이동. 모두 적용해도 수십 시간 규모로 **NFR-02(하루 이내) 달성은 불확실**하다.

→ **사용자 결정 (2026-08-02): 안 (d) 채택** — DCR-002 승인. NFR-02 수치는 재측정 후 확정 DCR로 정한다. 선택지였던 안: (a) NFR-02 완화(예: 2~3일) (b) v1 범위를 MODE-A1(전략 뷰, 자원 종류 없음)로 축소 (c) 영역 한정 스캔으로 축소 (d) 최적화 후 재측정하여 판단.

**권고: (d) — 단, 스파이크 게이트를 먼저 통과시킨다.**

| 순위 | 최적화 | 기대 효과 | 상태 |
|---|---|---|---|
| 1 | **이동을 좌표 점프 → 드래그 팬으로 전환** | 방문당 화면 전환 2회(약 3초)가 사라지고, **선택 팝업이 뜨지 않아** 커버 오프셋 56→90·보폭 4×14=56타일/방문. 방문 72,900→약 29,000, 방문당 약 2초 → **약 16시간** | 미검증 (`PostMessageInput.drag` 구현만 됨) |
| 2 | 분류 대상을 신규 커버 타일로 한정(199→36~56) | 3.2초 → 약 0.8초 | 미착수 |
| 3 | HUD 사각형을 실제 불투명 영역으로 축소 | 커버 오프셋 추가 증가 | 미착수 |

(b)는 DCR-001에서 "A1만으로는 자원 종류를 못 얻어 부족하다"고 판단해 A2를 택한 결정을 되돌리는 것이므로 권하지 않는다. (c)는 전체 맵 커버(AC-05)를 포기한다.

**게이트:** 드래그 팬 실현 가능성(팬 가능 여부, 1회 드래그당 이동량의 재현성, 누적 드리프트)을 짧은 스파이크(S-5)로 먼저 확인한다. 통과하면 (d)로 진행, 실패하면 (a)로 전환한다. 팬을 쓰더라도 드리프트 보정을 위해 행 시작마다 좌표 점프로 재앵커링한다.

## 검증

| 대상 | 방법 | 결과 | 증거 |
|---|---|---|---|
| S-1a 가림 캡처 | 오버레이로 창 전체 가림 + WGC 프레임 검사 | **성공** | `spikes/s1_background_io/evidence/` |
| S-1b 백그라운드 입력 | 관리자 권한 + 가림 상태 PostMessage 클릭 → 팝업 등장 | **성공** | before2.png / after2.png / screen_during_click.png |
| S-2 자원 식별 | 디테일·전략 뷰 클릭 판독 대조 | **P-01 실패**(전략 뷰 중립 미표시), 디테일 뷰는 가능 | `spikes/s2_zoom_grid/findings.md` |
| S-2c 종횡비 효과 | 3개 창 구성에서 클릭 좌표 폭 측정 | **가시 타일 85 / 89 / 246** | work3/snap_click_L,R.png |
| T10 store 계층 | `python -m unittest discover -s tests` (6 tests) | **전부 통과** | "Ran 6 tests ... OK" |
| T9 win 계층 (단위) | `python -m unittest discover -s tests` (20 tests: 사분면 계산, 권한 판정, 창 설정·복원 6경로, 창 확정 3경우) | **전부 통과** | "Ran 20 tests ... OK" |
| T9 win 계층 (실기) | 관리자 권한 `mapscan.cli probe --hwnd 0x503dc --click 700 350` | **통과** — 스캔 창 2544x657(종횡비 3.87) 설정, 캡처 바인딩 검증 2546x689, 클릭 후 변경 픽셀 4,695,626개(반응 확인), 창 배치 1244x730 복원 | `output/mapscan_run.log`, `output/probe.png`, `output/probe_after.png` |
| AC-07 오류 경로 복원 | 1차 실행에서 예외(UnicodeEncodeError) 발생 시에도 복원 동작 확인 | **통과**(의도치 않은 실증) | 1차 로그의 "창 배치 복원: 1244x730" |
| T9b 창 도구 | `-All`, `-Width/-Height`, `-Restore` 실기 + JSON 왕복 단위 5케이스 | **전부 통과** — `-All` 두 창을 (0,0)·(2560,0) 분배, `-Width 1600 -Height 900` 위치 유지 리사이즈, `-Restore` 복원·상태파일 삭제 | 커밋 `6fa804c` |
| T11 GridMapper (단위) | `tests/test_vision_grid.py` 11건 — 변환 왕복, 이웃 스텝, 셀 열거·제외, 실캡처 하이라이트 검출(중심 ±5px)·클릭→타일 매핑 | **전부 통과** | "Ran 40 tests ... OK" |
| T11 TileClassifier (회귀) | `tests/test_vision_classifier.py` 6건 — 고정 캡처(snap_tr_base, 같은 뷰 snap_click_R 앵커)에서 목재/공터/적군 2좌표/도시 파랑 2좌표/절벽·모래 미상/전체 셀 형식 | **전부 통과** | 동일 |
| T11 DigitReader | `tests/test_vision_digits.py` 3건 — 팝업 좌표 스트립 4소스 "(1020,620)" 등 완전 판독, read_coords 파싱, 빈 스트립 | **전부 통과** | 동일 |
| T11 완료 조건 | `mapscan.cli classify` — snap_tr_base 1장 → 가시 200타일 분류 CSV(미상 109·ally 61·enemy 25·공터 4·목재 1), 지상 진실 5좌표 대조 | **통과** (1.1초/프레임) | `output/classify_tr_base.csv` |
| T12 ScanPlanner (단위) | `tests/test_nav_planner.py` 10건 — 잉여류 보폭 조건, 전 좌표 커버, 결정적 재개, 보충 방문, 1619² 계획 생성 | **전부 통과** | "Ran 64 tests ... OK" |
| T12 Navigator (단위) | `tests/test_nav_navigator.py` 14건 — 프레임 오프셋, 지도 모드 판별·진입·포기, 안정화·시간초과, 점프 클릭 시퀀스(END+백스페이스)·앵커, 클램프 이진 탐색 3케이스 | **전부 통과** | 동일 |
| T12 실기 (S-3/FR-02) | 관리자 권한 `spikes/s3_nav_ui/live_check.py --hwnd 0x40148` | **통과** — 마커 0.097(일반 뷰)→지도 모드 진입, 맵 크기 **(1619,1619)** 39.7초, 점프 4.3초, 앵커 (1226,368), 199타일 분류 3.4초 | `spikes/s3_nav_ui/work/live_check.log`, `live_jump.png` |
| T12 앵커 정확도 | j1_jump의 타일 (1020,620)을 snap_tr_base 패치로 템플릿 매칭 | **일치** — 실측 (1226,368) vs 고정 앵커 (1226,368), y 오차 0.6px | `scratchpad/locate_tile.py` 재현 |
| T12 S-4 팝업 | 점프 후 ESC로 팝업 닫기 시도 | **불가** — ESC는 게임 종료 확인 창을 띄움(취소로 복구). 겹침 커버 유지 | `spikes/s3_nav_ui/work/popup_after.png`, `popup_check.log` |
| T12 안전장치 | 지도 모드 아님 상태에서 좌표 UI 클릭 시 | **차단 확인**(`NotInMapMode`) — 실기 오클릭으로 행군 화면이 열린 사고를 재발 방지 | `tests/test_nav_navigator.py::JumpTest` |
| T12 계정 교차 검증 | 테스트 클라이언트를 컬러단장용스(0x40148) → **羊커리(0x60042)**로 바꿔 동일 절차 재실행 | **동일 결과** — 맵 (1619,1619) 42.3초, 점프 3.1초, 앵커 (1226,368) 동일, 199타일 3.2초. UI 상수가 계정과 무관하고 창 크기에만 의존함을 확인 | `spikes/s3_nav_ui/work/live_check.log` |
| S-5 G1 팬 동작 | 羊커리 실기: 점프 (1020,620) 후 소폭 드래그 (-300,0), 전후 프레임 템플릿 매칭 | **통과** — 콘텐츠 (-256,0) 이동(NCC 0.637), 선택 링 없음, 점프 팝업 소멸(영역 변화율 0.993) | `spikes/s5_drag_pan/work/driver_run2.log`, `g1_before/after.png` |
| S-5 G2 재현성·관성 | 왕복 드래그 수평 12회·수직 4회 + 릴리스 후 0.5/0.8s 잔여 이동 측정 | **통과** — 좌팬 −443.7±0.5px(상단 대역), 관성 0.0px. 우측 게인 이상(0.62)은 HUD 가림 아티팩트로 판명(보조 패치 +444.2) | 동일 로그, `s5_results.json`, `analyze.py` |
| S-5 G3 드리프트·재앵커 | 재점프 후 좌팬 10연속 → 예측 타일 클릭 → 팝업 좌표 육안 대조 → 재점프 | **통과** — 클릭 대역 누적 이동(−4827px) 기준 mx 예측 1053.4 vs 실측 (1053,586) 정합. 균일 이동 가정은 팬당 수십 px 체계 오차(전단 확증) | `g3_click.png`, `g3_strip.png`, `analyze.py`, `analyze2.py` |
| S-6 재측정 (DCR-002 안 d) | 관리자 권한 인프로세스 `spikes/s6_remeasure/measure100.py` — 9행 × (점프 1 + 팬 10) = 방문 99회, 대역 한정 분류 + 커버 중복 제거 + HUD 축소 | **완료** — 총 320.6s, 신규 6,417타일. 광폭 팬 2.95s/68.2타일(23.1타일/s), 점프 6.7s/51타일. 1620² 외삽 **33~36시간** | `spikes/s6_remeasure/work/measure.log`, `measure_results.json` |
| T13 단위 (PanTracker) | `tests/test_nav_pan.py` 5건 — 전단 적합 복원, 2팬 누적, 오염 밴드 LOO 기각, 기대 창 밖 TrackLost, 게인 보간 | **전부 통과** | "Ran 74 tests ... OK" |
| T13 단위 (controller) | `tests/test_controller.py` 5건 — 행 계획 결정성·경계 인셋·c 커버, 대역 한정 분류+중복 제거+upsert, 팝업 제외, 미구현 모드 거부 | **전부 통과** | 동일 |
| T13 실기 (중앙 행) | 관리자 권한 `mapscan scan --hwnd 0x60042 --start-row 150 --max-rows 1 --max-pans 12` | **통과** — 점프 재앵커 + 광폭 팬 10회, **750타일/38s** 기록, 팬 #11 추적 손실 시 행 조기 종료(실패 0), 창 배치 복원 | `output/t13_check3.log`, `output/t13_check.db` |
| T13 실기 (경계 안전망) | 동일, `--start-row 268 --max-pans 6` (남쪽 경계 인접) | **통과** — 전단 기울기 이상(b=+0.39) 감지 → 좌표 오염 없이 조기 종료, 99타일 유지 | 동일 로그 |
| T14 이중 앵커 기각 (단위) | `tests/test_nav_pan.py` 실기 회귀 — 직전 대비 160px 변동이라도 게인 근방·상호 정합이면 유효 | **통과** ("Ran 78 tests ... OK") | `test_wide_pan_far_from_last_but_near_gain_is_kept` |
| T14 이중 앵커 기각 (실기) | 행 150 재실행(`--start-row 150 --max-rows 1 --max-pans 12`) — 팬별 텔레메트리 확보 | **개선 확인** — 구 규칙이 기각하던 정상 측정(팬 #7 밴드0) 유지. 조기 종료는 수면 저점수 팬(#10)으로 이동(사실 28) | `output/t14_check.db`, res 로그 |
| T14 점프 GO 재클릭·수면 폴백 (단위) | `tests/test_nav_navigator.py` 3건 — GO 무시 재현·재클릭, 포기 경로, 애니메이션 허용 | **통과** | JumpTest 신규 3건 |
| T14.2 서쪽 경계 앵커 | (0,922) 점프 후 화면 6점 클릭 → 팝업 좌표 대조 | **클램프 없음** — 5/6 정합(≤1타일), 1건은 판독 오류 추정 | `spikes/t14_pan_tuning/work/survey.json` |
| T14.4 기저 지역 의존 | 클릭-그리드 측량(center/rowstart/west) + 행 150 지상 진실 | **확인** — 확정 사실 26. 국소 ±1타일, 행 누적 시 팬당 my +1.3~1.7 | survey.json, click_top/bottom_strip.png |
| T14 재앵커 파일럿 | 행 150, 12팬, 3팬마다 클릭 재앵커 | **성공** — 드리프트 흡수·TrackLost 복구, 미적용 사이클만 잔여(≤(2,5)) | `spikes/t14_pan_tuning/work/pilot.json` |
| DCR-004 재앵커·지연 기록 (단위) | `tests/test_controller.py` 6건 — 드리프트 보간·커버 이동, 충돌·맵 밖 폐기, 버퍼 폐기 복원, 새니티 창 적용·기각 2경로 | **전부 통과** ("Ran 84 tests ... OK") | ReanchorFlushTest·ReanchorProbeTest |
| DCR-004 재앵커 (실기) | `mapscan scan --start-row 150 --max-rows 1 --max-pans 12` (controller v4) | **통과** — 재앵커 3회 적용(링 2·클릭점 1), 오판독 2회 새니티 차단(좌표 오염 0), 620타일 보정 기록·행 실패 0, 47s(오버헤드 ~0.75s/팬) | `output/t14_reanchor.db`, res_0007 로그 |
| T14.3 글리프 보강 (단위) | 봄 테마 스트립 2종(점령형·특수지형) 판독 회귀 — `tests/test_vision_digits.py` 확장, 기존 4소스 회귀 유지 | **전부 통과** ("Ran 85 tests ... OK") | 변형 6종(`1_3~5, 2_3, paren_l_3, paren_r_3`), `extract_glyphs.py` |
| T14.3+평지 우선 (실기) | 행 150 재실행(글리프 변형 + 최빈값 투표 + 평지 재앵커 + 창 완화) | **통과** — 재앵커 4/4 성공(드리프트 −1..−2, +0..+9), 꼬리 폐기 0, **816타일**·행 실패 0·46s. 직전 오판독 지점 (136,796) 정상 판독 | `output/t14_reanchor4.db`, res_0011 로그 |
| 이상 상태 안전성 (실기) | 게임 활동(행군·알림) 중 우연 실행 — 전 팬 추적 붕괴 상황 | **안전 확인** — 행 조기 종료, 오염 기록 0(전량 보수 폐기) | res_0009 로그, 사실 34 |
| 다중 행 연쇄 (실기) | `--start-row 150 --max-rows 3 --max-pans 12` — 행 종료(팝업 열림) → 다음 행 점프 연쇄 | **통과** — 3행 1,567타일/127s, 행 실패 0. 행 151 팬 #2 TrackLost 변위 유실(+10,−9)을 확장 새니티 창(±26)이 정확히 수용·복구 | `output/t14_rows.db`, res_0012 로그 |
| 재앵커 증거 수집 + 추가 변형 | 실패 스트립 자동 저장 → `extract_glyphs.py`로 변형 3종 추가(0_3·1_6·paren_l_4) | **판독 회복** — evidence_b "(104,836)" 정상 판독. evidence_a는 '0'-분리·"0)"-병합으로 잔존(사실 35) | `output/evidence/`, 커밋된 evidence_row151_a/b.png |
| T14.3 표본 라벨링 | 표본 10건 팝업 스트립 판독 → 지상 진실 라벨(`analyze_tiles.LABELS`) + 경계 밴드 색 분율 측정 | **완료** — 정탐 6건(적 4·아군 2), 오탐 2건(절벽·강/무주 공터 → ally), 소유 불확정 2건(패널 잘림) | `spikes/t14_pan_tuning/analyze_tiles.py` 출력 표 |
| 파랑 오탐 교정 (단위) | 마주보는 변 쌍 규칙 + 회귀 5건(`FieldSampleRegressionTest`) — 오탐 2건 중립 교정, 적·아군 정탐 유지, 기존 픽스처 도시 셀 유지 | **전부 통과** ("Ran 90 tests ... OK") | `tests/test_vision_classifier.py`, 픽스처 `work/tiles_*_cell.png` 5건 |
| 점령형 템플릿 (단위) | iron_occ·food_occ 등록 → 점령 철광 "철광"/점령 밭 "식량" 종류 판정, 기존 프레임 오매칭 없음(기존 회귀 유지) | **통과** — occ#2 (철광, ally, 1.0), mine#1 (식량, 1.0) | `register_templates.py` 재검증 출력 |
| FR-07 병합 (단위) | `StructureDetectionTest` 4건(성채 검출 위치·exclude·멤버 반경 기하·무관 지형 무검출) + `StructureMergeTest`(멤버 종류·center 공유·비멤버 유지) + `_flush` center 참조 보정 | **전부 통과** ("Ran 96 tests ... OK") | 픽스처 `work/tiles_occ_city.png`(도시 기준좌표 진실 (1016,626), 사실 36) |
| 판독 복구 (단위·코퍼스) | 증거 코퍼스 27장 재현(`read_evidence.py`) + `EVIDENCE_COORDS` 회귀 4건 | 코퍼스 **2/27→15/27**, 기존 판독 픽스처 무회귀 ("Ran 97 ... OK") | 사실 41, 커밋 `e06f829` |
| **지정 영역 스캔** (행 148~165) | `mapscan scan --db output/ac03.db` 6회 분할 실행(--new/--start-row/재개) — 18행, 판독 복구 전후 비교 | **완료** — **14,972타일**, 행 실패 0(조기 종료 행은 꼬리 폐기·보충 몫), PK 중복 0. 복구 전 행당 ~6팬 사망 → 복구 후 행 158이 50+팬 완주 | `output/ac03.db`, ac03_run1~6 로그 |
| **AC-03** 강제 종료→재개 | 행 163 팬 #3 진행 중 taskkill /F(WMI 분리 워처) → 재개 실행 → 잔여 행 완주 | **통과** — 체크포인트 163에서 재개, PK 중복 0, 미플러시 버퍼만 유실(지연 기록 설계 — 오염 0). 강제 종료 시 창 미복원은 사이드카 인계로 수정(아래) | `output/ac03_kill5.txt`("성공: 프로세스(PID 27592)가 종료되었습니다"), run5/run6 로그 |
| **AC-06** 가림 스캔 | 스캔 창 (2560,0) 2560×696을 최상위 오버레이로 완전 가림 + 실제 모니터 픽셀 캡처 → 행 158 진행 | **통과** — 가림 상태로 행 158 완주(50+팬, 재앵커 링·판독 정상), 화면 증거 `ac06_screen.png`(전면 검정 오버레이) | run4 로그 + `output/ac06_screen.png` |
| **AC-01** 좌표 이동·기록-팝업 대조 | 지정 영역 DB에서 7개 지점 점프 → 기록 셀 26클릭, 팝업 진실과 대조(`ac_check.py`, 자동 판독+수동 스트립) | **통과(제한부)** — 점프·화면 표시 정상. 좌표: 정합 8, ±1 픽 9(사실 42 — 기록 자체는 이웃 조회로 정합), 판독 불능·미개방 9(수동 대조로 보완). 분류 대조는 AC-02 행 참조 | `work/ac01_*.json` + 팝업 스트립, ac01_a~g 로그 |
| **AC-02** 분류 표본 대조 | 실기 표본 누적 대조 — 점령형(철광·식량·도시·공터 점령), 중립형(공터·식량·목재·석재·절벽·강), 점령상태(ally·enemy·neutral·mine·friendly) | **부분(미충족)** — 정탐: 공터 다수·식량 30·점령형 표본·도시 ally·적군 4. 보수 미상: 석재·Lv.2 목재 등(오분류 아님). **오분류: 중립 철광→공터 1**(사실 43)·mine 오탐 2(사실 44 — 규칙 수정으로 교정). 99% 판정은 중립형 자원 템플릿(철광·석재·구리) 확충 후 재대조 필요. 내땅·우호 정탐 표본 미확보 지속 | 사실 43·44, ac01 스트립·라벨 |
| **AC-05** 산출물 (부분) | 지정 영역 스캔 DB→CSV 내보내기(`export_csv`) 스키마·행 수 검사 | **기제 검증 통과** — 설계 스키마 12열 일치, 14,972행, UTF-8 BOM. 전 좌표 레코드 존재는 전체 스캔에서 판정 | `output/ac03_export.csv/scan_1_20260802.csv` |
| **AC-07** 창 복원 (보강) | 정상 6회·오류(T9 기존)·강제 종료 경로 — 강제 종료 시 원 배치 오인 결함 발견 | 정상·오류 **통과**(매 실행 "창 배치 복원" 로그). **강제 종료 결함 수정** — 원 배치 사이드카 영속화·재개 세션 인계(`winstate_*.json`), 단위 `test_original_rect_survives_hard_kill` | run 로그들, `session.py` 수정 |
| mine 오탐 교정 (단위) | 픽스처 `minefp_*_cell.png` 재채집(초록 0% 확인) + 합성 한쪽/마주보는 변 회귀 | **통과** ("Ran 99 tests ... OK") | 사실 44 |

## 구현 중 확정된 환경 제약 (재발 방지)

| 항목 | 내용 |
|---|---|
| PowerShell 스크립트 인코딩 | Windows PowerShell 5.1은 BOM 없는 `.ps1`을 ANSI(cp949)로 읽어 한글 리터럴이 깨지고 파싱이 실패한다. **한글이 들어간 `.ps1`은 UTF-8 BOM으로 저장** |
| Python 콘솔 출력 | 콘솔 기본 코드페이지(cp949)로 출력할 수 없는 문자가 있어 `cli.main()`에서 stdout/stderr을 UTF-8로 재설정한다 |
| PowerShell 배열 반환 | 함수가 단일 원소 배열을 반환하면 스칼라로 풀려 `+=`가 `op_Addition` 예외를 낸다. `return , $array` 로 고정 |
| PowerShell 파라미터명 | `-Args`는 자동 변수 `$Args`와 충돌해 값이 비워진다 |
| git 커밋 메시지 | 본문에 `$`·백틱이 있으면 PowerShell 히어독이 깨진다. 긴 메시지는 `git commit -F <파일>` 사용 |
| 상주 관리자 실행기 | 러너가 `& powershell -File ... > res` 방식으로 자식을 실행하므로, 자식이 Start-Process로 분리 기동한 스캔도 **파이프 핸들 상속으로 러너를 스캔 종료까지 블록**한다 — 동시 명령(킬 등)은 러너가 아니라 `Invoke-CimMethod Win32_Process Create`로 분리 기동한 워처가 수행해야 한다(AC-03 실증). res_/done_ 파일은 외부 정리로 사라질 수 있어 결과는 `output/` 파일로 남긴다 |

## 완료

(최종 보고 시 작성 — 현재 진행 중)
