# 설계 변경 기록

| 변경 요청 | 날짜 | 변경 요약 | 관련 ADR | 이전 기준선 | 새 기준선 | 승인자 | 상태 |
|---|---|---|---|---|---|---|---|
| [DCR-001](changes/DCR-001-mode-a-zoom-limits.md) | 2026-08-02 | 줌 렌더링 제약(전략 뷰가 중립 타일 미표시)에 따라 MODE-A를 A1(전략 줌: 점령 지도)·A2(디테일 줌: 자원 종류 포함)로 분리. **채택안: 안 1, 단 v1은 A2만 구현하고 A1은 후속.** 함께 창 크기 고정 전제를 폐기하고 넓고 낮은 창(사분면) 정책 도입 | ADR-004(유지), [ADR-005](decisions/ADR-005-window-aspect-ratio.md)(신규·승인) | v1 | **v2** | 사용자 | **승인** |
| [DCR-002](changes/DCR-002-scan-throughput.md) | 2026-08-02 | T12 실기 실측 결과 MODE-A2 전체 스캔이 약 150시간으로 **NFR-02(하루 이내)에 미달**. 원인은 선택 팝업·HUD 오클루전으로 방문당 신규 커버가 36타일뿐인 것과 방문당 소요 7.7초. 선택지 (a)NFR-02 완화 (b)MODE-A1로 축소 (c)영역 한정 (d)최적화 후 재측정. **권고 (d)** — 이동을 드래그 팬으로 전환(팝업 미발생·화면 전환 제거)해 약 16시간을 목표로 하되, 팬 실현 가능성을 스파이크 S-5로 먼저 확인 | ADR-004·005(유지) | v2 | 미정 | — | **제안됨 (결정 대기)** |

## 경미한 변경 (재승인 불필요, 이유 기록 후 진행)

구현 중 확정된 사실로 승인된 설계의 **수단**만 바뀌고 요구사항·인수 조건·공개 계약은 그대로인 항목이다(wf-implement §4.1).

| 날짜 | 변경 | 이유·증거 | 반영 위치 |
|---|---|---|---|
| 2026-08-02 | 타일 격자 모델을 2:1 등각 마름모 → **일반 평행사변형**으로 | 실측 기저 `E_MX=(99.5,49.0)`, `E_MY=(-55.5,45.0)`. 계획서의 `a≈82.4, b≈a/2` 공식은 실제 렌더링과 불일치 | 설계 §2 GridMapper, `vision/grid.py` |
| 2026-08-02 | 그리드 앵커를 선택 하이라이트 링 → **점프 후 고정 화면 위치** | 좌표 점프 후에는 링이 약하거나 늦게 나타나 신뢰할 수 없다. 게임이 대상 타일을 고정 위치(클라이언트 1225,337)에 놓는다. 템플릿 매칭 교차 검증 y 오차 0.6px | 설계 §4.3, `nav/ui.py`, `nav/navigator.py` |
| 2026-08-02 | 맵 크기 감지를 글리프 OCR → **클램프 렌더링 이미지 비교 이진 탐색** | P-03 실패(입력란 폰트는 숫자가 붙어 있어 글리프 분리 불가). [ADR-006](decisions/ADR-006-map-size-detection.md) | 설계 §4.2, `nav/navigator.py` |
| 2026-08-02 | WGC 캡처 바인딩을 제목 매칭 + z-raise → **HWND 직접 바인딩**(`window_hwnd`) | 같은 제목·같은 크기 창을 구분하지 못해 조용히 다른 창을 캡처했다. S-1이 규정한 "HWND 기준 바인딩"의 올바른 구현 | [ADR-002](decisions/ADR-002-capture-input.md), `win/capture.py` |
| 2026-08-02 | 방문 계획을 사각형 타일링 → **잉여류 타일링 + 커버리지 비트맵 보충** | 오클루전으로 커버 집합에 구멍이 생겨 최대 사각형만으로는 커버 효율이 떨어진다 | 설계 §2 ScanPlanner, `nav/planner.py` |
| 2026-08-02 | 모든 지도 UI 클릭 전 **지도 모드 마커 확인**을 의무화 | 지도 모드가 아닌 상태의 오클릭으로 행군(부대 이동) 선택 화면이 열린 실기 사고(ESC로 취소, 행군 미실행). S-2 발견 #5가 예견한 위험 | 설계 §4.1·§4.4, `nav/navigator.py` |

## DCR-001 상세

**변경 사유의 증거:** 스파이크 S-2 (`spikes/s2_zoom_grid/findings.md`)

1. 전략 뷰는 중립 타일(자원·공터)을 렌더링하지 않는다 → 광역 줌 1패스로 자원 종류를 얻을 수 없다.
2. 게임의 월드 렌더 배율은 창 높이를 따르므로 가시 타일 수는 종횡비에 비례한다 → 창을 사분면 크기(넓고 낮게)로 두면 가시 타일이 85 → 246개로 약 2.9배가 된다.

**승인에 따른 후속 조치**

- [x] DCR-001 상태 → 승인
- [x] ADR-005 신규 작성 → 승인 (ADR-001~004는 v1 상태 유지, 대체 관계 없음)
- [x] 요구사항 명세 v1 → v2 (모드 표, FR-03a/b, NFR-01, NFR-02, 인수 조건)
- [x] 설계 문서 v1 → v2 (컴포넌트, 스키마 값, 성능 추정, 추적표)
- [x] 구현 계획(plan.md) 갱신 — 보류했던 T13을 A2 기준으로 재개
