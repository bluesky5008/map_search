# S-7 — MuMu 에뮬레이터(B안) 게이트 4·5 스파이크 판정

- 일자: 2026-08-02 (밤)
- 대상: MuMu 인스턴스 '용스' (top `0x1b1ee2`, MuMuNxDevice `0xa03b6`, nemudisplay `0xba0076` — 실행 시점 재확인, 재시작 시 변동)
- 결론: **게이트 4 통과, 게이트 5 통과.** B안 정식 전환의 기술 차단 요소 없음 → DCR-005 기안.

## 게이트 4 — WGC 캡처 + PostMessage 입력: 통과

| 항목 | 결과 | 증거 |
|---|---|---|
| WGC 바인딩 | **top(Qt 창)만 가능** — 프레임 2546×698. 자식(MuMuNxDevice/nemudisplay)은 `GraphicsCaptureItem` 변환 실패(WGC는 최상위 창만) | `cap_probe.py`, work/cap_probe.json |
| 게임 영역 원점 | **프레임 (1,40), 2544×657 @1:1** — PC 기준 UI 패치(지도·전보 버튼) 전역 매칭 NCC 0.88~0.90으로 역산, 두 패치 일치. `frame_client_offset(frame, (2544,657))`이 정확히 (1,40)을 산출 → **제품 client_offset 무변경 성립** | `analyze_frame.py`, work/analyze_frame.json |
| PostMessage 클릭 | **MuMuNxDevice가 수신**(빈 지면 클릭 → 팝업 "공터 (346,313)" 전환·육안 확인). nemudisplay는 무반응. top은 미확정(선행 클릭과 동일 타일이라 판별 불가) — 입력 대상은 device로 확정 | `click_probe.py`, work/click_after_*.png |
| 격자 기하 | 클릭 변위 역산 예측 (345,314) vs 팝업 진실 (346,313) — **PC 기저(E_MX/E_MY)와 ±1타일 정합**(픽 오차 범위, 사실 42) | 본 문서 산출 |
| 드래그 팬 | 소폭 (-300,0) → **-258px**(PC -256과 동일 게인 0.86). 좌팬 3회 **-443.0 σ0.0**(PC -443.7±0.5), 우팬 2회 **+446 σ0**(보조 패치 — 기본 패치 저점수는 S-5와 동일한 우측 HUD 가림 아티팩트). **관성 0px**(+0.5s 잔여) | `drag_probe.py`·`drag_verify2.py`, work/drag_*.json |
| 수직 팬 | 하팬 (0,+250) 전후 같은 화면점 클릭 좌표 진실 대조: 실측 (-3,-3) vs 예측 (-1.7,-3.0) — 정합 | `drag_vertical.py`, work/drag_vertical.json |
| 팝업 거동 | 첫 팬에서 선택 팝업 소멸(팝업 영역 변화율 0.884) — S-5 사실 16과 동일 | work/drag_g1_*.png |
| 권한 | MuMu는 **비관리자** — 일반 셸에서 직접 실행, UAC·상주 실행기 불필요 | 전 스크립트 비상승 실행 |

## 게이트 5 — 좌표 글리프 DigitReader 1:1 재사용: 통과

- 글리프 절대 크기 **1:1 확정** — 육안 확정 스트립 "(350,306)"의 9글자 중 7자가
  PC 템플릿과 즉시 정합(정렬 성공 자체가 크기 동일의 증거).
- 잔여 오판독은 **소스별 변형**(확립 메커니즘, 사실 19·32와 동일 유형):
  '3'→'8' 2건, ')'→'0' 1건 → MuMu 변형 3종 수확(`3_2.png`, `3_3.png`, `paren_r_4.png`).
- 변형 포함 검증: MuMu 2소스 정판독 + **PC 회귀 11소스 전부 무잠식**
  (`harvest_glyphs.py` — 임시 디렉터리 `work/digits_mumu/`, 제품 자산은 DCR 승인 후 등록).
- 현장 추가 검증: 수직 팬 검사의 (350,305)·(347,302) 판독 성공 — '2','4','5','7'도 정판독.
- 링 검출(`find_selection_highlight`)은 이 장면에서 미검출(타일 크기 성분 부재) —
  제품 재앵커는 클릭점 폴백이 있어 치명적이지 않으나 **적응 후 실사 필요**(아래).

## 관찰·제약 (적응 단계 반영)

1. **캡처 = top, 입력 = MuMuNxDevice** — 캡처/입력 HWND 분리 필요(현행은 단일 hwnd).
2. **`apply_scan_rect` 생략 필수** — 자식 클라이언트 2544×657 = 내부 해상도(1:1).
   리사이즈 시 스케일링으로 1:1 파괴. 창 종횡비 경고도 부적용.
3. MuMu 창 발견: 제목 필터 미검출 — **Qt top + MuMuNxDevice/nemudisplay 자식 구조**로
   판별(`cap_probe.find_mumu_instances` 참조 구현).
4. `client_offset`: top 프레임에 CLIENT_SIZE=(2544,657) 적용 시 기존 공식이 (1,40)을
   산출 — 탭바 40px가 자연 흡수돼 **무변경**. 단 `_verify_binding`의 창 외곽 대조
   (2546×698 vs 2552×701)는 허용 오차 24px 이내라 이 역시 무변경 성립.
5. 선택 팝업에 **"군사 스킬" 칩이 선택 타일 아래(~+150px)에 부착**(계정/콘텐츠 차이
   추정) + 링 미검출 장면 존재 — 커버 오프셋·재앵커 링 신뢰도 실사 필요.
6. 게이트 2(에뮬레이터 허용) 감시: 스파이크 전 과정에서 경고 팝업 등 이상 징후 없음
   (검증은 아니며 장시간 운용에서 계속 감시).
7. 스파이크는 그랩마다 WGC 세션을 새로 열었다(1회성 프로브 편의). 제품은 기존
   상시 세션(free-threaded) 그대로.

## 적응 구현·처리율 실측 (DCR-005 승인 후, 2026-08-02)

### 추가 확정 사실

| # | 사실 | 대응 |
|---|---|---|
| A1 | **좌표 입력란 클릭은 전면 IME 오버레이(상단 전폭 텍스트 바)를 연다** — 지도 배너 마커가 가려져 `_click_verified`가 중단(안전장치 정상). 오버레이는 WM_CHAR 숫자를 **덧붙이기만** 하고, lParam=0 키 메시지(VK_BACK·VK_END·Enter)와 더블탭 전체선택은 무효 | Navigator `ime_overlay` 모드: 개폐를 마커 점수로 감지(−0.02/0.99), 확정은 ✓ 버튼 클릭(`ui.IME_CONFIRM`), `_set_coords` 블러 클릭 생략(✓=커밋) |
| A2 | **스캔코드를 채운 WM_KEYDOWN/UP은 오버레이에서 동작한다** — VK_BACK×6으로 프리필 완전 삭제 실증(ime_probe4) | `PostMessageInput.key()`가 lParam에 MapVirtualKeyW 스캔코드를 채우도록 수정(PC 경로는 lParam=0도 처리했으므로 호환) |
| A3 | 선택 팝업 구성이 계정·타일 의존 — **PC(컬러단장용스) 점프 팝업에도 정찰 패널(rel x −477..−320)·군사 스킬 칩 존재**(j1_jump 재실측). MuMu 클릭 팝업은 4버튼+4아이콘 구성 확인 | `POPUP_RECTS_REL` **부위별 다중 사각형**으로 전환(정찰 패널·상단 밴드·버튼 열·칩). 합집합 단일 사각형은 좌상 오프셋 (−4,0)을 죽여 **맵 상단 경계 커버 불성립**(planner 실측) — 정찰 패널 상단 ~25px는 의도적 미모델(경계 셀 모서리 슬리버 허용) |
| A4 | 선택 링(크림색)은 MuMu 3장면 모두 미검출(타일 크기 성분 부재) | 재앵커는 클릭점 폴백(기존 경로)으로 동작 — 실측에서 재앵커 정상 |
| A5 | 맵 크기 감지(blur 클램프 렌더링 비교)는 오버레이 개폐와 얽혀 **미지원** | `detect_map_size`가 ime_overlay에서 명시 거부 — `--map-size 1619 1619` 지정(같은 월드 PC 실측값) |

### 처리율 (행 100·101, K=4, `measure_mumu.py`)

| 행 | 결과 | 비고 |
|---|---|---|
| 100 | **완주 — 135s, 2,951타일, 21.81타일/s**, 팬 43, 재앵커 10/13, 드리프트 정상 범위 | **PC 실측(21.3~21.9)과 동등** |
| 101 | 조기 종료(팬 22, 18.32타일/s, 1,272타일) — 팬 23~26 연속 추적 손실(저점수 0.08~0.15) | PC의 콘텐츠성 플레이크(사실 25·34)와 동일 유형. 오염 기록 0, 안전 동작 정상 |

**외삽**: 처리율이 PC와 동등 → 단일 인스턴스 전체 스캔 **38~52h(중심 ~44h)** 유지.

## 다계정 병렬 (DCR-005 후속 구현·실측, 2026-08-02 심야)

- 구현: `scan --end-row`(행 청크 상한 — 체크포인트 재개 유지, 보충 방문 생략),
  `mapscan merge`(청크 DB 병합 — 같은 좌표는 captured_at 최신 우선, 병합 스캔
  체크포인트를 전체 행 수로 설정해 **재개 시 곧장 보충 방문 단계로 진입**),
  `mapscan chunks --accounts N`(분할 안내·명령 출력). 단위 4건(총 115건).
- **2인스턴스 동시 실측** (용스 행 104~105 + 실버 행 210~211 동시 실행):

  | 인스턴스 | 행 | 결과 |
  |---|---|---|
  | 용스 | 105 | 132s/2,951타일 = **22.4타일/s** — 단독 실측(21.8)과 동등, **병렬 저하 없음** |
  | 용스 | 104 | 추적 손실 3회 조기 종료(289타일) — 콘텐츠성(사실 25 유형) |
  | 실버 | 211 | 61s/738타일 — 재앵커 연속 실패로 조기 종료 |
  | 실버 | 210 | 재앵커 판독 실패 2연속, 0타일 — **남서부(my 1200~1300) 렌더링 판독 실패 지역**(사실 41·45 유형, 증거 4장 자동 축적 — 수확·보강 대상) |

- merge 실기: 3,240 + 738 = **3,978타일**(겹침 없음 정합), 맵 크기 검증 통과,
  checkpoint 544. 로그: output/par_yong.log·par_silver.log
- **병렬 견적(확정)**: 2계정 저하 없음 실측 → **2계정 20~27h**. 4계정+(11~15h)은
  인스턴스 2개 환경이라 동시 저하 미실측 — 확장 시 행 2개 실측으로 보정.
- 잔여: 병합 후 "재개 → 보충" 실기 경로는 전체 스캔에서 실증(단위·상태 검증 완료).
  실버 계정(羊커리)은 같은 월드 확인(부대 패널 좌표 체계 동일).

## 원격(RDP)·모니터 off 운용 점검 (2026-08-02 심야, `health_check.py`)

- 모니터 off + RDP 접속 상태에서 **두 인스턴스 모두 정상**: 창 구조·위치 불변,
  프레임 생동(1초 변화율 0.07 — 캡처·렌더링 갱신 중), 행 106 기능 검사에서
  점프(지도 진입·IME 입력·GO)·팬·분류(731타일)·재앵커 성공 2회 — 전 경로 동작.
- RDP 렌더링은 로컬 대비 미세 저하(UI 패치 NCC 0.90 → 0.75) — 임계 여유 내
  (지도 마커 0.7 임계 통과는 점프 성공으로 실증). health_check 기준 0.7로 반영.
- 행 106 조기 종료(재앵커 판독None 연속)는 RDP와 무관한 **서부(x 30~350,
  y 400~630) 지역 판독 실패** — 어제 모니터 on 병렬(행 104·105)과 동일 모드.
  증거 자동 축적 중(수확·보강 대상). 재시도·보충 몫.
- **미검증 잔여**: RDP **연결 해제(disconnected) 상태**의 장시간 렌더링 지속
  여부 — 야간 무인 운용 전 "스캔 걸어두고 RDP 끊기 → 재접속해 로그 확인" 1회
  필요. RDP 클라이언트 해상도가 기존 데스크톱과 다르면 창 재배치 위험(이번
  세션은 동일해 무사).

## 재현 순서

```
python spikes/s7_mumu/enum_windows.py            # 읽기 전용 열거
python spikes/s7_mumu/cap_probe.py 용스          # WGC 바인딩 (캡처만)
python spikes/s7_mumu/analyze_frame.py           # 원점 역산 + 판독 시험 (오프라인)
python spikes/s7_mumu/harvest_glyphs.py          # 변형 수확 + 검증 (오프라인)
python spikes/s7_mumu/click_probe.py 용스        # 클릭 1~3회
python spikes/s7_mumu/drag_probe.py 용스         # 팬 반응·재현성
python spikes/s7_mumu/drag_verify2.py 용스       # 우팬·수직 보조 재측정
python spikes/s7_mumu/drag_vertical.py 용스      # 수직 팬 좌표 진실 대조
python spikes/s7_mumu/ui_survey.py               # 팝업 기하 실사 (오프라인)
python spikes/s7_mumu/ime_probe.py ~ ime_probe4.py  # IME 오버레이 진단 이력
python spikes/s7_mumu/measure_mumu.py --mumu 용스   # 처리율 실측 (제품 경로)
```
